# SH 实验脚本执行规范

本文档说明“算力调度台”当前对实验 `.sh` 脚本的要求、执行方式和注意事项。

## 1. 基本结论

普通的 Linux Bash 脚本通常可以直接提交，但并不是任意一个“可执行文件”都一定能够正常运行。

脚本需要满足以下条件：

- 使用 Linux 环境可以识别的 Bash 语法；
- 脚本依赖的代码、数据集、模型和配置文件已经存在于服务器上，或者由脚本自行下载；
- 配置的远程工作目录存在，并且当前 SSH 用户具有读写权限；
- 依赖的 Python、CUDA、系统命令和其他软件已经安装；
- 脚本能够正确返回退出码。

## 2. 文件格式要求

### 推荐格式

- 文件扩展名使用 `.sh`；
- 使用 UTF-8 编码，最好不要带 BOM；
- 使用 Unix/Linux 换行符 `LF`，不要使用 Windows 换行符 `CRLF`；
- 第一行推荐写明解释器：

```bash
#!/usr/bin/env bash
```

### 是否需要本地执行权限

不需要在 Windows 本地提前设置执行权限。

客户端上传脚本后会在服务器上自动设置权限，并通过 Bash 执行。脚本本身仍然必须是有效的 Bash 脚本。

## 3. 上传范围

当前客户端只会自动上传你在界面中选择的 `.sh` 文件，不会自动上传：

- Python 或其他源代码；
- 数据集；
- 模型权重；
- 配置文件；
- 整个项目目录；
- Conda 环境和 Python 依赖。

脚本会被上传到服务器当前用户主目录下的任务目录，路径类似：

```text
~/.gpu-orchestrator/runs/<任务ID>/run.sh
```

因此，脚本引用的其他文件需要提前放到服务器上，或者在脚本中自行复制、解压或下载。

## 4. 远程工作目录

提交任务时配置的“工作目录”是服务器上的 Linux 路径，不是 Windows 本地路径。

脚本执行前，客户端会先进入该目录。例如配置：

```text
/home/username/my-project
```

脚本中的相对路径就会以该目录为基准：

```bash
python -u train.py --config configs/exp.yaml
```

使用相对路径时，请确认相关文件确实位于配置的工作目录中。

如果工作目录不存在或没有权限进入，任务会启动失败，并记录失败原因。

## 5. Conda 环境

提交任务时可以从客户端检测到的 Conda 环境下拉框中选择环境。

- 选择具体环境：使用该 Conda 环境运行脚本；
- 选择“默认 Python”：不额外指定 Conda 环境，使用服务器当前可用的默认 Python；
- 不建议在脚本中再次执行 `conda activate`；
- 如果实验依赖 PyTorch、CUDA 或特定版本的 Python 包，应明确选择对应的 Conda 环境。

例如，任务配置选择了 `pytorch` 环境后，脚本中可以直接写：

```bash
python -u train.py
```

不需要再写：

```bash
conda activate pytorch
```

## 6. GPU 使用方式

调度器会根据服务器、GPU 空闲状态、显存门槛、执行级别和 GPU 基准速度选择 GPU，并自动设置：

```bash
CUDA_VISIBLE_DEVICES=<被分配的GPU>
```

脚本不需要、也不建议自行覆盖这个变量。

在 PyTorch 中，当前任务被分配的 GPU 通常会以逻辑编号 `0` 出现：

```python
import torch

device = torch.device("cuda:0" if torch.cuda.is_available() else "cpu")
```

注意事项：

- 当前一个实验任务默认分配一张 GPU；
- 多卡分布式训练脚本目前不能仅靠一个普通任务自动获得多张 GPU；
- 脚本中硬编码其他物理 GPU 编号，可能导致任务使用错误的显卡；
- 不要在脚本中把 `CUDA_VISIBLE_DEVICES` 改成其他值。

## 7. 脚本执行建议

推荐使用以下写法：

```bash
#!/usr/bin/env bash
set -euo pipefail

echo "开始实验"

python -u train.py \
  --config configs/exp.yaml \
  --output outputs/exp1

echo "实验完成"
```

### `set -euo pipefail`

推荐使用：

- `set -e`：命令失败时尽早退出；
- `set -u`：使用未定义变量时报错；
- `set -o pipefail`：管道中的前置命令失败时也返回失败。

这样可以让调度器更准确地识别任务失败。

### 后台进程

不建议脚本自行使用 `nohup`、`&` 等方式把主要任务放到后台：

```bash
python train.py &
```

这种写法可能导致主脚本提前退出，调度器会误以为实验已经完成。

如果确实需要启动后台子进程，请在脚本最后等待它结束：

```bash
python worker.py &
worker_pid=$!
wait "$worker_pid"
```

## 8. 日志和退出状态

脚本的标准输出和标准错误都会统一保存到任务日志中，日志位于服务器：

```text
~/.gpu-orchestrator/runs/<任务ID>/run.log
```

客户端的“运行日志”页面可以查看日志内容。

任务状态主要根据以下信息判断：

- 远程进程是否仍然存在；
- 任务退出码；
- 任务日志中的错误信息；
- 是否检测到显存不足特征。

### 退出码要求

- 退出码 `0`：任务标记为“已完成”；
- 非 `0` 退出码：任务标记为“执行失败”；
- 检测到 CUDA OOM、`out of memory`、退出码 `137` 或 `139` 等情况时，可能进入等待显存状态并自动提高显存门槛；
- 不同程序的错误日志格式不完全相同，因此无法保证识别所有类型的失败。

如果训练程序启动了子进程，建议确保主脚本等待所有子进程结束，并让主程序的退出码正确传递出来。

## 9. 暂停、重新等待和重连

### 暂停任务

- 等待中的任务会直接变为“暂停中”；
- 执行中的任务会暂停远程进程；
- 任务不会被删除，日志和任务记录会保留。

### 全部重新等待

点击“全部重新等待”时：

- 暂停中的等待任务重新进入等待队列；
- 暂停中的运行任务会先终止当前暂停进程，再重新进入等待队列；
- 调度器下一次轮询时会重新选择服务器和 GPU。

### 客户端关闭

客户端关闭时不会主动终止已经在服务器上运行的任务。远程任务会继续执行。

客户端下次启动并重新连接服务器后，会根据任务日志、退出码文件和远程进程状态同步任务结果。

## 10. 磁盘空间要求

当前调度器会检查服务器当前用户主目录所在文件系统的可用空间。

当可用空间低于当前安全阈值 5GB 时：

- 客户端显示磁盘空间告警；
- 该服务器的任务调度会暂停；
- 等待任务会进入“暂停中”；
- 正在运行的任务会被暂停；
- 磁盘空间恢复后，需要手动点击“全部重新等待”或单独恢复任务。

建议预留足够空间用于：

- 实验日志；
- 模型检查点；
- 临时文件；
- 数据集缓存；
- Conda 或 Python 程序产生的缓存。

## 11. 提交前检查清单

提交任务前建议确认：

- [ ] 脚本使用 Bash 语法；
- [ ] 文件编码为 UTF-8，换行符为 LF；
- [ ] 配置的远程工作目录存在；
- [ ] 工作目录中的代码、数据和配置文件齐全；
- [ ] 选择了正确的 Conda 环境；
- [ ] 没有覆盖 `CUDA_VISIBLE_DEVICES`；
- [ ] 主要程序以前台方式运行；
- [ ] 后台子进程已经使用 `wait`；
- [ ] 脚本可以正确返回退出码；
- [ ] 服务器主目录有足够可用空间；
- [ ] 任务需要的模型、数据和输出目录具有读写权限。

## 12. 完整示例

```bash
#!/usr/bin/env bash
set -euo pipefail

PROJECT_DIR="/home/username/my-project"
OUTPUT_DIR="${PROJECT_DIR}/outputs/exp-001"

mkdir -p "${OUTPUT_DIR}"
cd "${PROJECT_DIR}"

echo "开始时间: $(date '+%Y-%m-%d %H:%M:%S')"
echo "CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES:-未设置}"

python -u train.py \
  --config configs/exp.yaml \
  --output "${OUTPUT_DIR}"

echo "结束时间: $(date '+%Y-%m-%d %H:%M:%S')"
```


